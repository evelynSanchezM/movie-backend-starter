{
  (providers = [
    {
      type: "static",
    },
  ]),
    (build = {
      commands: ["apt-get update", "apt-get install -y curl jq"],
    }),
    (start = {
      command: "./load-data.sh",
    });
}
